# Iteration Workspace README

## Purpose

本目录用于承载每个版本的需求工作过程。

## Structure

- `_Template/`: 新版本模板

说明：
当前目录默认只保留模板骨架，不长期保留示例版本。

## Usage Rule

1. 复制 `_Template/`
2. 重命名为新版本号，例如 `v1.1`
3. 按阶段补全文档
4. 稳定后回填到 `01_Master_Documents/`
