# 🎨 Phase 4A: 高保真原型原生开发 (Antigravity Native Prototype)

> ⚠️ **强制双端装载指令 (Dual-Role Pre-requisite)**
> 在执行本阶段前，你必须静默读取并融合：
> 1. `Agent_Roles/UX_Design.md` (交互体验负责人：把控信息层级与微交互)
> 2. `Agent_Roles/Prototype_Design.md` (原型与结构负责人：把控高保真页面结构与交互表达)
  3. `00_Playbook/PRD_Standard/Pattern_Specs/UI_Aesthetic.md` (美学红线)
---

## 📸 视觉引导门禁 (Visual Gating)
- **动作**：AI 必须停下并提示：“指挥官，请上传 [当前页面/模块] 的视觉参考图或手绘草图。”
- **指令**：在收到图片后，AI 必须执行 `[视觉特征提取]`：
  1. 识别图片中的间距（Padding/Gap）风格。
  2. 识别图片中的色彩占比与按钮样式。
  3. 识别图片中的对齐方式（左对齐/居中/通栏）。
- **目标**：将提取的视觉特征与 PRD 逻辑进行像素级缝合。
---

## 🛑 表达红线 (Prototype Expression Lock)
- 严格保持页面结构、状态流和交互层级的一致性。
- 严禁引入与当前版本无关的视觉风格或无关展示元素。

## 🔄 执行动作：双核驱动组装 (Structure and UE Injection)

### 🟢 动作 1：SaaS 骨架与原型表达
- **标准布局**：采用经典的左侧 Sider + 顶部 Header + 自适应 Content 布局。
- **品牌基因**：全局主题色强制设为 **Daybreak Blue (#1890ff)**。
- **状态流转**：明确基础的页面切换、弹窗交互和状态反馈，让原型“活”起来。

### 🟡 动作 2：高保真数据注入 (High-Fi Mocking)
- 严禁使用 `test`、`123` 等无意义数据。
- 必须读取 Phase 3 定义的 Mock 数据字典，注入逼真的行业数据（如：真实运单号 `TRW-20260327-001`、合理的 GPS 坐标、口岸名称）。

### 🔴 动作 3：UE 体验审查与注入 (UE Review)
- **防呆与防错**：破坏性操作（如删除、撤回）前，强制加入二次确认。
- **微交互设计**：强制实现 Loading 状态、空状态提示、成功/失败提示。
- **认知减负**：复杂表单强制分步（Steps）或分组；高频操作放置在第一视觉落点。

### 🔵 动作 4：环境激活与实时预览 (Preview Activation)
- **自动指令**：原型说明输出完毕后，AI 必须立刻执行：
  `打开当前版本 Prototype_Assets 中的当前原型并执行预览`
- **目标**：确保原型预览处于可验收状态，实现“内容落位即可预览”。

### 🔴 动作 5：红线对标质检 (Red Line Audit)
- **要求**：在生成原型说明前，AI 必须检查布局是否符合 `Quality_Standards.md` 的 Sider/Header 规范。
- **要求**：检查主色调是否已锁定为 #1890ff。
---

## 🔄 执行动作：轻量视觉表达优化 (Clean-Flow Injection)

### 🟢 动作 1：去框化布局组装 (Borderless Layout)
- 遵循 `UI_Aesthetic.md`，移除沉重的实体边框。
- 使用 **Negative Space (负空间)** 进行逻辑分区，而非“画格子”。

### 🟡 动作 2：高保真数据与状态 (Dynamic Mocking)
- 注入符合跨境物流真实场景的 Mock 数据。
- 实现 Tabs 切换、抽屉展开、按钮 Loading 等实时交互，拒绝死图。

### 🔴 动作 3：流程通顺质检 (Flow Audit)
- 检查页面是否出现了“大模块堆砌”？如果是，立即通过增加垂直间距（Gap）和弱化次要元素进行“消肿”。
- 确保核心动作（如提交、保存）始终处于用户的“视觉舒适区”。
---

## 📦 本阶段交付物 (Deliverables)

- **输出 1：实时预览 (Live View)**
  - **路径**：`/02_Iteration_Workspace/v[当前版本号]/Prototype_Assets/`
  - **作用**：供指挥官在右侧预览区即时验收、点击、交互。

- **输出 2：版本快照 (Version Snapshot)**
  - **路径**：`/02_Iteration_Workspace/v[当前版本号]/Prototype_Archive/`
  - **作用**：作为本版本的原型快照留存。
  - **要求**：该文件必须与实时预览版完全一致，严禁任何字节偏差。
  

- **全量覆盖禁令**：拒绝 `// ...此处省略`，所有输出都必须完整表达页面结构、交互说明与资产路径。
- **汇报与流转**：
  > “[系统日志：Phase 4A 原型已就绪] 
  > 报告指挥官，原型已同步存入快照库，预览资产已就绪。请在右侧预览区直接验收。确认体验顺畅后，是否确认进入 Phase 5？”
  - **后置动作**：按全局规则更新工作记录。
