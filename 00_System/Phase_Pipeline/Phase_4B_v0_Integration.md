# 🧩 Phase 4B: v0 高保真原型接入与缝合 (v0 Integration)

> ⚠️ **强制双端装载指令 (Dual-Role Pre-requisite)**
> 在执行本阶段前，你必须静默读取并融合：
> 1. `Agent_Roles/UX_Design.md` (交互体验负责人：审查外部原型 UI 的合理性)
> 2. `Agent_Roles/Prototype_Design.md` (原型与结构负责人：负责结构收敛、状态表达与原型缝合)
> 3. `00_System/PRD_Standard/10_UI_Aesthetic_Standard.md` (👈 新增：美学红线)

## 📸 视觉引导门禁 (Visual Gating)
- **动作**：AI 必须停下并提示：“指挥官，请上传 [当前页面/模块] 的视觉参考图或手绘草图。”
- **指令**：在收到图片后，AI 必须执行 `[视觉特征提取]`：
  1. 识别图片中的间距（Padding/Gap）风格。
  2. 识别图片中的色彩占比与按钮样式。
  3. 识别图片中的对齐方式（左对齐/居中/通栏）。
- **目标**：将提取的视觉特征与 PRD 逻辑进行像素级缝合。

## 🛑 核心禁令 (The Hard Lock)
- **禁止盲目复制**：严禁直接把外部原型原封不动丢进项目。必须经过结构和表达清洗。
- **严禁数据真空**：必须剔除 v0 默认的 "John Doe" 类占位符，替换为我们的业务域 Mock 数据。

## 🔄 执行动作：结构清洗与逻辑接管 (Refactor and Stitching)

### 🟢 动作 1：资产清洗与结构映射
- 接收指挥官提供的外部原型或初始界面资产。
- **交互替换评估**：如果 v0 中的某些界面区块（如复杂表格、分页、日期选择、状态反馈）表达过于简陋，必须补充为更完整的企业级交互说明。保留 v0 中有效的卡片布局和视觉组织方式。

### 🟡 动作 2：状态机唤醒 (State Wiring)
- 外部原型通常缺乏真实的交互流转。你必须补足状态变化、页面路径和异常反馈。
- 绑定抽屉、弹窗、表单校验和 Tab 切换的视图流转说明。

### 🔴 动作 3：UE 规范与高保真数据强行覆盖 (UE & Data Override)
- **强制防呆**：检查外部原型中的危险按钮，补全二次确认拦截。
- **状态补全**：为静态界面补全 Loading 态和无数据态。
- **数据注入**：读取 Phase 3 的 Mock 数据字典，注入真实的 `TRW-` 运单流水号、时间戳和业务枚举值。全局高亮色强转为 **Daybreak Blue (#1890ff)**。

### 🔵 动作 4：逻辑接管后的实时预览 (Live Preview)
- **自动指令**：清洗与缝合完成后，AI 必须立刻执行：
  `打开 03_Prototype_Assets 中的当前原型并执行预览`
- **目标**：让外部原型资产在本地预览环境下快速可用。

### 🔴 动作 5：红线对标质检 (Red Line Audit)
- **要求**：在生成原型说明前，AI 必须检查布局是否符合 `Quality_Standards.md` 的 Sider/Header 规范。
- **要求**：检查主色调是否已锁定为 #1890ff。
---
## 🔄 执行动作：欧美简约主义实现 (Clean-Flow Injection)

### 🟢 动作 1：去框化布局组装 (Borderless Layout)
- 遵循 `10_UI_Aesthetic_Standard.md`，移除沉重的实体边框。
- 使用 **Negative Space (负空间)** 进行逻辑分区，而非“画格子”。

### 🟡 动作 2：高保真数据与状态 (Dynamic Mocking)
- 注入符合跨境物流真实场景的 Mock 数据。
- 实现 Tabs 切换、抽屉展开、按钮 Loading 等实时交互，拒绝死图。

### 🔴 动作 3：流程通顺质检 (Flow Audit)
- 检查页面是否出现了“大模块堆砌”？如果是，立即通过增加垂直间距（Gap）和弱化次要元素进行“消肿”。
- 确保核心动作（如提交、保存）始终处于用户的“视觉舒适区”。
---

## 📦 本阶段交付物 (Deliverables)

- **输出 1：实时预览 (Live View)**
  - **路径**：`/03_Prototype_Assets/`
  - **作用**：供指挥官在右侧预览区即时验收、点击、交互。

- **输出 2：版本快照 (Version Snapshot)**
  - **路径**：`/02_Iteration_Workspace/v[当前版本号]/Prototype_Archive/`
  - **作用**：作为本版本的原型快照留存。
  - **要求**：该文件必须与实时预览版完全一致，严禁任何字节偏差。

- **全量覆盖禁令**：拒绝 `// ...此处省略`，两处路径均必须输出完整的结构说明、页面资产和交互补充内容。
- **汇报与流转**：
  > “[系统日志：Phase 4B 原型接管完成] 
  > 报告指挥官，外部原型已完成规范化清洗并同步备份，预览资产已更新。请检查逻辑缝合后的效果。是否授权进入 Phase 5？”
- **后置动作**：按全局规则更新工作记录。
