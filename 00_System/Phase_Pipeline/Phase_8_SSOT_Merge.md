# 🧬 Phase 8: SSOT 逻辑注入 (Master Merge)

> ⚠️ **强制双端装载指令 (Dual-Role Pre-requisite)**
> 在执行本阶段前，你必须静默读取并融合：
> 1. `/02_Agent_Roles/08_architect.md` (首席架构师：把控全局逻辑一致性与拓扑结构)
> 2. `/02_Agent_Roles/09_steward.md` (数据资产管理员：负责 Master 档案的无损缝合与修订追溯)

---

## 🛑 核心禁令 (The Hard Lock)
- **严禁暴力覆盖**：绝对不允许用迭代文档直接覆盖 Master 文档。Master 是神圣不可侵犯的。
- **严禁误删旧政**：注入新逻辑时，必须保护 Master 文档中与其他模块相关的历史有效逻辑不被篡改或删除。
- **差异化冲突检查**：若发现本次迭代逻辑与既有 Master 逻辑冲突，必须立刻拦截并请求指挥官裁决。

## 🔄 执行动作：全局真相缝合 (Truth Synthesis)

### 🟢 动作 1：全局需求母体缝合 (Global PRD Merge)
- **增量提取**：精准提取本次迭代 `/02_Iterations/v[当前版本号]/PRD_Final.md` 中的所有新增或修改的逻辑点（重点：五栏式拆解、状态机路径）。
- **定位与注入**：在 `/01_Master_SSOT/Global_PRD_Master.md` 中寻找对应的章节锚点（如 `2.1 调度工作台`），将增量内容**无损缝合**进去。
- **修订追溯**：必须在 Master 文档顶部的【修订记录表】中自动新增一行：
  - `[2026-03-31] - v[当前版本号] 逻辑已合并注入 - @Architect`

### 🟡 动作 2：系统架构图实时重绘 (Architecture Sync)
- **增量扫描**：扫描本次迭代是否引入了新的大模块、API 接口或第三方依赖。
- **图纸更新**：打开 `/01_Master_SSOT/Architecture.md`，自动更新其中的 **Mermaid 架构拓扑图**，确保图纸与系统当前状态 100% 对齐。

### 🔴 动作 3：指挥官进度看板刷新 (Dashboard Refresh)
- **状态流转**：打开 `/01_Master_SSOT/Dashboard.md`。
- **数据更新**：
  - 将本次任务的状态从 `🟡 处理中/Phase 7` 更新为 `🟢 已就绪/Phase 8 已合并`。
  - 同步更新“最近风险提示”或“技术债”等项目健康度指标。

---

## 📦 本阶段交付物 (Deliverables)

- **更新资产**：全量更新后的 `/01_Master_SSOT/` 目录文件。
- **汇报与流转**：
  > “[系统日志：Phase 8 逻辑注入完毕 🧬] 
  > 报告指挥官，v[当前版本号] 增量逻辑已完美缝合至全局档案。系统单一真相（SSOT）已同步至最新状态。
  > 架构图与进度看板已刷新。是否授权开启最终的 **Phase 9 (Git 自动同步)**？”

- **后置动作**：按全局规则更新 `workthrough.md`。